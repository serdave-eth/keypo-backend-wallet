# Master key for HKDF key derivation - MUST be at least 32 characters
MASTER_KEY=your-very-secure-master-key-minimum-32-characters-long-change-this

# JWT Secret for session management - MUST be at least 32 characters
JWT_SECRET=your-very-secure-jwt-secret-minimum-32-characters-long-change-this

# Database URL (PostgreSQL)
# Format: postgresql://username:password@host:port/database?schema=public
# Use this with the included docker-compose.yml:
DATABASE_URL="postgresql://keypo:keypo_password@localhost:5432/keypo_wallet?schema=public"

# WebAuthn Configuration
WEBAUTHN_RP_NAME="Keypo Wallet"
WEBAUTHN_RP_ID="localhost"
WEBAUTHN_ORIGIN="http://localhost:3000"

# Node Environment (development, production, test)
NODE_ENV=development